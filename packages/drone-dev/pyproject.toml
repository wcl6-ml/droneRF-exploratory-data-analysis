[project]
name = "drone-dev"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "wcl6-ml", email = "weichung.lai6@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "h5py>=3.16.0",
    "numpy>=2.2.6",
    "requests>=2.34.2",
]

[project.scripts]
drone-dev = "drone_dev:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
